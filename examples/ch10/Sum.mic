// generic module: not compiled standalone, instantiated by UseSum.mic
// tag::sum[]
module Sum(T: integer)
procedure sum*(const a: ^[]T; n: integer): T
var i: integer; s: T
begin
    s := 0
    for i := 0 to n-1 do s := s + a[i] end
    return s
end sum
end Sum
// end::sum[]
